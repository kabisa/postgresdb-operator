- name: BACKUP PLAYBOOK
  hosts: localhost
  tasks:
    - name: Include vars
      include_vars:
        file: vars.yml
    - name: ensure no old version of cron job
      include_role:
        name: jobs
        tasks_from: backup
      vars:
        job_type: job